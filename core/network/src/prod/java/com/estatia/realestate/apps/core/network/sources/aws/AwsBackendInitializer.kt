package com.estatia.realestate.apps.core.network.sources.aws

import com.estatia.realestate.apps.core.network.interfaces.IBackendInitializer
import javax.inject.Inject

/**
 * AWS implementation of [IBackendInitializer] (Skeleton).
 */
internal class AwsBackendInitializer @Inject constructor() : IBackendInitializer {
    override fun initialize() {
        // Future: Amplify.addPlugin(...)
        // Future: Amplify.configure(context)
    }
}
