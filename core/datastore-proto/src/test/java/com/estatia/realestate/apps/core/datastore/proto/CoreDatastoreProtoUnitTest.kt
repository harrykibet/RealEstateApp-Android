package com.estatia.realestate.apps.core.datastore_proto

import org.junit.Test
import org.junit.Assert.assertEquals

/**
 * Unit tests for the datastore-proto module (run on JVM).
 * Add tests that use generated proto classes (e.g. UserPreferences) when needed.
 */
class CoreDatastoreProtoUnitTest {

    @Test
    fun placeholder_assertsCorrect() {
        assertEquals(4, 2 + 2)
    }
}
