package com.estatia.realestate.apps.benchmark

import org.junit.Test
import org.junit.Assert.assertEquals

/**
 * Unit tests for the benchmark module (run on JVM).
 * Benchmark logic that can be tested without a device can go here.
 */
class BenchmarkUnitTest {

    @Test
    fun placeholder_assertsCorrect() {
        assertEquals(4, 2 + 2)
    }
}
