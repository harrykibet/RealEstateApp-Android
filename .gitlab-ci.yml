stages:
  - test

test-job:
  stage: test
  image: ubuntu:latest
  script:
    - echo "Testing GitLab CI/CD"
    - echo "Running basic commands"
    - uname -a
    - echo "Pipeline execution successful!"
